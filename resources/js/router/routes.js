const routes = Object.create({
    characters: 'characters',
    character: 'character',
    register: 'register',
    login: 'login',
    notFound: 'notFound',
})

export default Object.freeze(routes);
